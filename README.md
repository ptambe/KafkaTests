# A simple Kafka Test in scala

This is a simple test code which starts an embedded zookeeper and kafka server and can be used to write integration tests for your kafka code.

Use SBT to build and run tests
